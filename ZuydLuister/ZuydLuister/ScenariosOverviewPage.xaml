<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZuydLuister.ScenariosOverviewPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="scenarioListView" ItemTapped="scenarioListView_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding}" Style="{StaticResource}"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="newScenarioButton" Text="Nieuw Scenario Toevoegen" Clicked="newScenarioButton_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>